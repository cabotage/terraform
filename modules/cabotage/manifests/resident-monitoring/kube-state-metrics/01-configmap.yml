apiVersion: v1
kind: ConfigMap
metadata:
  name: resident-kube-state-metrics-web-config
  namespace: cabotage
data:
  web-config.yml: |
    tls_server_config:
      cert_file: /etc/tls/tls.crt
      key_file: /etc/tls/tls.key
