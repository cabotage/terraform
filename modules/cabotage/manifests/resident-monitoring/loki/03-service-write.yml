apiVersion: v1
kind: Service
metadata:
  name: resident-loki-write
  namespace: cabotage
  labels:
    app: resident-loki
    component: write
spec:
  selector:
    app: resident-loki
    component: write
  ports:
  - port: 3100
    targetPort: https
    name: https
  - port: 9095
    targetPort: grpc
    name: grpc
