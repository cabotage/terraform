apiVersion: v1
kind: Service
metadata:
  name: resident-loki-memberlist
  namespace: cabotage
  labels:
    app: resident-loki
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: resident-loki
  ports:
  - port: 7946
    targetPort: memberlist
    name: memberlist
    protocol: TCP
