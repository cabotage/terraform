apiVersion: v1
kind: Service
metadata:
  labels:
    app: consul
  name: consul-api
  namespace: cabotage
spec:
  ports:
  - name:
    port: 443
    protocol: TCP
    targetPort: 8443
  selector:
    app: consul
  type: ClusterIP
  publishNotReadyAddresses: true
