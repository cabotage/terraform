apiVersion: v1
kind: Service
metadata:
  namespace: cabotage
  name: registry-ingress
  labels:
    app: registry
    cabotage.io/infra: "true"
spec:
  ports:
    - port: 5000
      name: registry
  selector:
    app: registry
