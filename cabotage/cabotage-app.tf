# --- Cabotage Application ---
# Step 14 in start-cluster
#
# Replaces: manifests/cabotage/cabotage-app/
#           scripts/cabotage/cabotate-app/boostrap-vault-cabotage-app
#
# TODO: Port cabotage-app deployment (web, worker, beat), service,
#       configmap, enrollment, vault bootstrap, DB/Redis URI config
