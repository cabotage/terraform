apiVersion: v1
kind: Service
metadata:
  name: resident-loki-read
  namespace: cabotage
  labels:
    app: resident-loki
    component: read
spec:
  selector:
    app: resident-loki
    component: read
  ports:
  - port: 3100
    targetPort: https
    name: https
  - port: 9095
    targetPort: grpc
    name: grpc
